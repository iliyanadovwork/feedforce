# dependencies
node_modules/

# builds
.next/
out/
build/

# env / secrets
.env
.env.local
.env*.local

# Local Claude session context — never commit
CONTEXT.md

# Local QA notes — never commit
ISSUES.md
QA_CHECKLIST.md

# Local Remotion promo-video subproject — never commit
video/

# Local AI-session tooling (agent skills, Claude settings/worktrees, skills lockfile) — never commit
.agents/
.claude/
skills-lock.json
.vercel

# misc
.DS_Store
next-env.d.ts
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
